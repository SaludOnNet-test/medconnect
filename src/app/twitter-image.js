// Twitter / X re-uses the Open Graph generator since the brand card is identical.
// Kept as a separate file so Next.js auto-injects <meta name="twitter:image">.
export { default, alt, size, contentType } from './opengraph-image';
