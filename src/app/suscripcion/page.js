/**
 * /suscripcion — temporarily hidden during MVP
 * Plan content is preserved in the `feature/with-plans` git branch.
 * Re-enable by merging that branch into main.
 */
import { redirect } from 'next/navigation';

export default function SuscripcionPage() {
  redirect('/');
}
